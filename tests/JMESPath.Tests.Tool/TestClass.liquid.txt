public class {{#name}}
{
{% for test in tests %}
    [Theory]
{% for case in cases %}
    [InlineData("{{{#case.expression}}}","{{{#case.stringResult}}}")]
{% endfor %}
    public async Task Test(string expression, string json)
    {
        JsonNode node = JsonNode.Parse(json);
    }
    
{% endfor %}
}